import { Box, Typography } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <Box>
            <Typography variant='h5'>React + Mui Boilerplate</Typography>
        </Box>
    );
};

export default Home;
